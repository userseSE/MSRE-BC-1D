# Work stream
## version 1: use `odeint` lib to solve ode and compilable version
## version 2: get results align what in python program
## version 3: write ode solver
## version 3: modify parameters and reasonable results
## version 4: use struct instead of `extern` in parameters (rearrange the structs)
## version 5: use array, instead of vectors (static, use `dense` instead of `eigen`, only basic operants can be used)
## version 6: write a solver to replace `SparseLU`
## version 7: test more sets of export temperature sets from simulations of brayton cycle
## version 8: align all the results details with article diagrams, and reserve some dataset logs